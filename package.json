{
	"name": "basehub-mux-demo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "basehub && wrangler deploy",
		"dev": "basehub dev & wrangler dev",
		"test": "vitest",
		"typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.15.2"
	},
	"dependencies": {
		"basehub": "^8.2.7",
		"hono": "^4.7.9"
	}
}